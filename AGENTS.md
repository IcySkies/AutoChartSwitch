# Build Requirement

After modifying this project, build the Release solution before finishing work:

```powershell
dotnet build AutoChartSwitch.sln --configuration Release
```

Resolve any build errors introduced by the change before handing the work off.
